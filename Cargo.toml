[package]
name = "faos-cli"
version = "0.1.0"
edition = "2021"
description = "CLI for a free and open S-Platform (The world's largest PC gaming platform)."
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
regex = "1.11"
